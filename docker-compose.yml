version: '3.8'

services:
  bandhq:
    build: .
    container_name: bandhq
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - bandhq_db:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db

volumes:
  bandhq_db:
