version: '3.8'

services:
  bandhq:
    build: .
    container_name: bandhq
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data/dev.db:/app/prisma/dev.db
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - BAND_PASSWORD=moondyne2026
